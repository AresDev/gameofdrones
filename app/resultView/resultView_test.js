'use strict';

describe('GameOfDrones.resultView module', function() {

  beforeEach(module('GameOfDrones.resultView'));

  describe('resultView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var resultViewCtrl = $controller('ResultViewCtrl');
      expect(resultViewCtrl).toBeDefined();
    }));

  });
});